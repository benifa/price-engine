# Hugging Face (required for Llama tokenizer + Hub datasets; Modal uses huggingface-secret)
HF_TOKEN=

# OpenAI (optional; priceengine eval --frontier gpt-5)
OPENAI_API_KEY=

# Weights & Biases (optional; set wandb.enabled: true in a training config)
WANDB_API_KEY=
WANDB_PROJECT=price-engine
